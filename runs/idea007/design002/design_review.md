## design002
**Verdict:** APPROVED
**Feasibility evidence:**
- `_sobel_mag_luma` reuses `rgb_to_yuv` (parent `code/losses.py:25`) and `_SOBEL_X`/`_SOBEL_Y` (parent `code/losses.py:31-37`); no new dependency. Returning a non-detached differentiable Sobel magnitude is mechanically the same conv path the parent already uses inside `sobel_jnd_mask` (parent `losses.py:59-61`), only without the `@torch.no_grad` decorator and applied to `(stego - cover)` instead of `cover`.
- Insertion site exists: parent's `forward` computes `w = 1.0 / (1.0 + self.jnd_beta * M)` at `code/losses.py:135` and `l_pixel = (w * (stego - cover).abs()).mean()` at `losses.py:139`. Inserting the TV block immediately after `l_pixel` inside the `if self.jnd_beta > 0.0:` branch (parent `losses.py:130-141`) is a clean addition. `w.shape == (B,1,H,W)`; `w_max = w.view(B,-1).amax(dim=1)` is a one-line reduction with no shape risk.
- `l_quality` is composed at parent `code/losses.py:157-160`; appending `+ self.tv_w * l_tv` adds a differentiable term whose gradient flows through `(stego - cover)` since `_sobel_mag_luma` is not under `no_grad`.
- The `logs` dict at parent `code/losses.py:163-171` already adopts `key=tensor.detach()` style; adding `l_tv=l_tv.detach()` mirrors the existing `l_pixel=l_pixel.detach()` line at `losses.py:170`.
- Constructor change is additive (`tv_w`, `tv_eps` defaults 0.0 / 1e-6) — backwards compatible with existing callers; the `ImageSecretLoss(...)` call at parent `code/exp0_inn_train.py:258-266` only needs `tv_w=0.3` appended.
**Idea contradiction check:** The idea's direction (2) prescribes single-scale Sobel mask kept, `tv_w * mean((1 - w_norm) · |∇(stego - cover)|)` on luma residual, `w_norm = w / max(w)` per-image, `tv_w` in [0.2, 0.5]. design002 chooses `tv_w=0.3` (mid-range), uses the parent's existing single-scale `sobel_jnd_mask`, computes the residual gradient with the same Sobel kernel on luma. All clauses match. Idea's hard constraint that the TV term operate on the residual `(stego − cover)` not on `stego` is honored explicitly in §6.
**Strongest objection (ruled out):** Could differentiating `sqrt(gx² + gy² + 1e-12)` produce numerical instability (very large gradients near zero residual)? The `+1e-12` floor makes the gradient bounded; design002 §7 calls this out, and `_sobel_mag_luma` runs under autocast like the parent's mask helper. Also: clean bit_acc tail risk acknowledged with `bit_acc_clean_min < 0.99` as the flag-to-Designer threshold.
