## design003
**Verdict:** APPROVED
**Feasibility evidence:**
- All design001 edits feasible per design001 review (parent `code/losses.py:25, 31-37, 48-67, 96-116, 130-141`); all design002 edits feasible per design002 review (parent `code/losses.py:135, 139, 157-160, 163-171`).
- Composition: the multi-scale `w` produced by the new `multiscale_sobel_jnd_mask` has the same `(B,1,H,W)` shape and `(0,1]` value range as the parent's single-scale `w` (parent `losses.py:135`), so the TV block's `w.view(B,-1).amax(dim=1).clamp_min(self.tv_eps)` reduction is shape-compatible. Single mask-compute-per-forward is preserved (mask is computed once at the new dispatch site replacing parent `losses.py:131-134`, then reused in YUV, masked-L1, and TV terms).
- Constructor combines both kwarg sets additively; the `__init__` body remains a flat list of `self.x = x` assignments — no ordering or interaction risk.
- `ImageSecretLoss(...)` call site at parent `code/exp0_inn_train.py:258-266` accepts the four new kwargs (`jnd_mode`, `jnd_sigmas`, `jnd_pct`, `tv_w`) by name. Per-step jsonl emission site exists.
**Idea contradiction check:** Idea's direction (3) prescribes "stack 1 and 2 ... use the multi-scale Sobel mask `w_multi` everywhere (YUV term, masked-L1 term, and the complementary TV term's `w_norm`)". design003 §4.1 explicitly enforces "the TV term's `w_norm` is computed from the *multi-scale* `w` (not from a single-scale mask)" — exact match. Parent settings preserved (β=4.0, `pixel_w=0.5`, `secret_weight=20`, `lpips_w=0`).
**Strongest objection (ruled out):** Combined intervention has the largest deviation surface from the parent — could it push `bit_acc_clean_min` materially below the parent's 0.9949 (already below 1.0)? §6 explicitly flags this: "Combined intervention has the largest deviation surface from the parent — if a smoke run shows `bit_acc_clean_min < 0.99` at step 5000+, flag to Designer (would suggest reducing `tv_w` first since the TV term is the harder structural prohibition)." This is the right escalation policy and matches the idea constraint. No silent retune.
