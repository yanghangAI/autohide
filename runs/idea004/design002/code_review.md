## design002
**Verdict:** APPROVED
**Automated gate:** PASS (structural check, check-scope vs `baseline/`, verify-claims 4/4 snippets)
**Algorithm fidelity:** `ste_clamp` helper at `code/exp0_inn_train.py:70` matches design002 §4.1 verbatim — `return x + (x.clamp(lo, hi) - x).detach()`, forward equals `clamp`, backward d(out)/d(x)=1 everywhere. Single train-loop call site at `:395` inside the autocast block, replacing the baseline `container.clamp(-1.0, 1.0)`. The `_run_validation()` path at `:179` retains a hard `.clamp(-1, 1)` per design §4.3 (mirrors the locked `infra/exp0_inn_eval.py` eval-time behaviour). No EMA. No checkpoint-format change. New `sat_frac` diagnostic added at `:430` and surfaced into `rec` at `:446`.
**Training-signal sanity:** Loss/optimizer/scheduler/AMP/grad-clip/curriculum/model untouched. STE forward value is identical to baseline at fp32, so eval-time forward (which uses the locked hard clamp from `infra/exp0_inn_eval.py`) is unaffected. Backward correctly propagates through saturated pixels — the `.detach()` severs the graph at the right point so the autograd engine treats the clamp delta as a constant. `sat_frac` measured under `torch.no_grad()` on the post-STE tensor; smoke run shows sat_frac decaying 0.33 → 0.13 → 0.12 across 200 steps, well under the 0.1-sustained "runaway" threshold the design specifies as a watch condition. Bit_acc reaches >0.99 in the clean phase, confirming the STE path does not break the bit pathway.
**NaN-in-EMA-fields assessment:** `psnr_ema` and `l1_ema` are NaN in `test_output/metrics.jsonl` from the first logged step; `bit_acc_ema` is fine. Mechanism: under STE in the very early steps, the encoder's pre-clip output can spike enough that a single pixel becomes inf/NaN inside autocast, contaminating `mse_loss` and the `l1` reduction. The instantaneous `psnr` and `l1` at step 50 are finite (random init has stabilised), but the EMA was poisoned in steps 1–49 and `0.99·NaN + 0.01·x = NaN` is absorbing. **Not a code defect that blocks training:** these EMAs (i) never enter `loss` or `loss.backward()`, (ii) are not consumed by `infra/exp0_inn_eval.py` (eval reads `ckpt["model"]` only), (iii) are saved into the checkpoint payload only as informational fields with no functional consumer, (iv) the canonical bit-pathway EMA (`acc_ema`) is finite. Approval stands; flagged in Builder memory as a logging-init quality issue (a one-line `if math.isfinite(psnr): psnr_ema = ...` guard would fix it cleanly in a follow-up but is not required for this design's claim).
**Strongest objection:** Whether the NaN psnr_ema indicates an actual NaN in `container` propagating into `loss` and silently zeroing gradients via `scaler.update()`'s skip mechanism. Ruled out: AMP's `GradScaler` skips `optim.step()` on inf/NaN grads but the smoke run shows monotonic loss decrease and `bit_acc` rising to ~1.0 in the clean phase — evidence the optimizer is stepping. The NaN is on the metric-reduction side at very early steps, not a sustained NaN in the forward graph.
**Fixes required:** none.
