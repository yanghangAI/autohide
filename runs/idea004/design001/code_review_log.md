## 2026-04-26T17:48:49+00:00 — Code review
## design001
**Verdict:** APPROVED
**Automated gate:** PASS (structural check, check-scope vs `baseline/`, verify-claims 7/7 snippets)
**Algorithm fidelity:** Code matches design.md §4 verbatim. `WeightEMA` class at `code/exp0_inn_train.py:70` implements fp32 shadow with `state_dict`/`load_state_dict`, `update()` outside autocast using `p.detach().float()`, and `copy_to()`. Constants at `:295-296` (`EMA_DECAY=0.999`, `EMA_WARMUP_STEPS=args.stage_a_steps`). EMA constructed at `:315`. Resume path at `:357-367` reads `model_online` for the optimizer-tied weights and only falls back to `model` when the dual-slot key is absent, then restores EMA state. EMA updates only when `global_step >= EMA_WARMUP_STEPS` at `:480-481`. `save_ckpt()` writes EMA weights into the eval-consumed `model` slot post-warmup (`:415-426`), preserves `model_online` for resume, and merges in non-parameter buffers — preserving the eval contract that `infra/exp0_inn_eval.py` reads `ckpt["model"]`. `ema_active` field added to `rec` at `:517`.
**Training-signal sanity:** Loss formulation, optimizer, scheduler, AMP, grad-clip unchanged. EMA update is post-`sched.step()`, after the optimizer step; it sees the just-updated online weights — correct ordering. Decay 0.999 with 25k post-warmup updates gives ~693-step half-life, so the saved EMA is dominated by converged-region weights as design says. Buffers copied verbatim each step (INNCodec has no BN; future-proof). `test_output/metrics.jsonl` shows finite `psnr_ema`, `l1_ema`, `bit_acc_ema` from step 50 onward; `ema_active=1` (smoke-test `stage_a_steps` < 50). Smoke train completed 200 steps cleanly.
**Strongest objection:** Whether EMA-into-`ckpt["model"]` corrupts the eval-consumed slot if a save lands at exactly `step == stage_a_steps`. Ruled out: at the first post-warmup step the EMA shadow has had one update with weight `(1-d)=0.001` from the online weights and is otherwise the *initial* online clone — i.e. essentially the online weights at construction. Eval still sees a valid checkpoint. The non-parameter buffers are merged in from `model.state_dict()` so no model attribute goes missing.
**Fixes required:** none.

