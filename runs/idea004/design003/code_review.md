## design003
**Verdict:** APPROVED
**Automated gate:** PASS (structural check, check-scope vs `baseline/`, verify-claims 10/10 snippets)
**Algorithm fidelity:** Code is the union of design001 and design002 verbatim, applied to the same baseline starting point as the design specifies. `WeightEMA` at `code/exp0_inn_train.py:70` and `ste_clamp` at `:130` — identical to the singletons. EMA constants at `:316-317`, EMA construction at `:336`, dual-slot resume at `:378-386`, STE call site at `:483` (single train-loop replacement for `container.clamp(-1.0, 1.0)`; validation-path clamp at the same `_run_validation` location preserved as hard clamp), EMA update at `:499-500` (after `sched.step()`, before diagnostics), `save_ckpt()` dual-slot payload at `:434-446` (EMA into `ckpt["model"]` post-warmup, online into `ckpt["model_online"]`, plus `ema` state_dict). Both diagnostic fields `ema_active` and `sat_frac` present in `rec` at `:537+`. The two interventions touch disjoint surfaces (EMA = optimizer/checkpoint side; STE = forward graph) so there is no cross-contamination.
**Training-signal sanity:** Per-axis sanity checks from design001 and design002 carry over. EMA update reads online weights *after* the STE-trained step has been applied; the EMA shadow is therefore a low-pass filter of the STE-driven trajectory exactly as the design's §8 interaction discussion describes. `test_output/metrics.jsonl` shows the same NaN-in-`psnr_ema`/`l1_ema` pattern as design002 (and `bit_acc_ema` finite); `sat_frac` decays 0.33 → 0.19 → 0.12 → 0.12 across the 200-step smoke run. `ema_active=1` confirms the EMA path is exercised. Smoke run completes cleanly.
**NaN-in-EMA-fields assessment:** Same root cause and same disposition as design002 — `psnr_ema`/`l1_ema` are diagnostic-only fields with no consumer in `loss`, `optim.step`, `save_ckpt`'s eval-consumed slot, or the locked eval scripts. Specifically here, the EMA *of model weights* (the actual mechanism this design is testing) is computed elementwise on parameter tensors, completely independent of the metric-EMA scalars; a NaN in `psnr_ema` cannot leak into `WeightEMA.shadow`. Acceptable to ship.
**Strongest objection:** Whether EMA averaging the STE-driven trajectory could produce checkpoint weights that fail `bit_acc_clean = 1.000` even when the online weights pass — i.e. EMA-of-trajectories is not weight-equivalent to the trajectory's endpoint and might land in a different basin. Ruled out as a code-review concern: this is exactly the empirical question design003 is constructed to answer (and the design explicitly flags it as a finding to report rather than to pre-correct). At the code level, the EMA-warmup-from-stage_a_steps mitigation is correctly wired (`:499-500`) and matches the idea's risk-mitigation prescription. The result on the eval grid will adjudicate; reviewing further would be pre-judging an experimental outcome.
**Fixes required:** none.
