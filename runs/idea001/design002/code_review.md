## design002
**Verdict:** APPROVED
**Automated gate:** PASS (review-check-implementation: structural PASS; check-scope PASS against parent baseline/; verify-claims PASS, 4 snippets)
**Algorithm fidelity:** Code matches design.md §4. `ImageSecretLoss` instantiated with `secret_weight=args.bit_weight, yuv_w=0.5, lpips_w=3.0, ffl_w=0.5` at `code/exp0_inn_train.py:256-261` — exactly the LPIPS-dominant rebalance specified in §3/§4.1. Loss-call replacement is structurally identical to design001 (`quality_alpha = args.img_weight * attack_alpha`, fp32-wrapped `img_loss_mod(...)` call, no double-counted BCE) at lines 388-402. Logging additions per §4.3 present at lines 449-451. `losses.py` unchanged from baseline as required.
**Training-signal sanity:** (1) Test mini-train confirms LPIPS dominates: at step=200 `l_lpips=0.532`, `l_yuv=0.069`, `l_ffl=0.030` — ratios match the 3:0.5:0.5 weight schedule (LPIPS contributes ~3*0.532 ≈ 1.60 of total l_quality 1.65). (2) `secret_weight=20.0` preserved → bit pressure unchanged from baseline; `bit_acc` reached ~0.99 in ramp phase. (3) Curriculum semantics intact: `quality_alpha` follows attack_alpha, 0 in clean phase. (4) Optimizer/scheduler/AMP/resume/grad-clip untouched. (5) `metrics.jsonl` and `ckpt_latest.pt` produced; metrics fields populated as specified.
**Strongest objection:** With `lpips_w=3.0` dominating the quality bundle, `bit_acc_clean=1.000` could be at risk if the larger perceptual gradient shrinks the residual budget below the bit-payload threshold. Ruled out by: (a) `secret_weight=20.0` is unchanged from baseline (whose `bit_acc_clean=1.0` was verified in `results.csv`), and (b) BCE term enters as `20.0 * l_bce`, while quality enters as `1.0 * (3*l_lpips + 0.5*l_yuv + 0.5*l_ffl)`; with l_quality observed ~1.6 at end of ramp and l_bce typically <<1 once bits learned, the BCE-driven gradient remains dominant for bit recovery. Approved; final eval at step 30000 will be the definitive check.
