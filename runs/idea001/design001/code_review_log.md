
## 2026-04-26 — Code review verdict: APPROVED
## design001
**Verdict:** APPROVED
**Automated gate:** PASS (review-check-implementation: structural PASS; check-scope PASS against parent baseline/; verify-claims PASS, 4 snippets)
**Algorithm fidelity:** Code matches design.md §4 verbatim. `ImageSecretLoss` instantiated with `secret_weight=args.bit_weight, yuv_w=1.5, lpips_w=1.0, ffl_w=1.5` at `code/exp0_inn_train.py:256-261`. Inline L1+BCE replaced by single fp32-wrapped `img_loss_mod(...)` call at `code/exp0_inn_train.py:389-402` (`quality_alpha = args.img_weight * attack_alpha`; inner `autocast(enabled=False)` block per §4.3). `bit_loss` and `img_loss` re-derived from `loss_logs["l_bce"]` and `loss_logs["l_quality"]` (no double-counting of BCE). Logging extended with `l_yuv/l_lpips/l_ffl/l_quality/quality_alpha` at lines 446-450 per §4.4; pre-existing fields (`l1_container_cover`, `secret_rms`, `secret_rec_rms`, etc.) preserved. `losses.py:67` still computes `total = self.secret_weight * l_bce + alpha * l_quality`, confirming the single call evaluates the full design loss.
**Training-signal sanity:** (1) `quality_alpha=0` during clean phase (verified in test_output/metrics.jsonl: step=50 has `quality_alpha=0.0` while `attack_alpha=0.0`; step=100 ramps to 0.49 then 1.0) — clean-phase pure-bit pressure preserved. (2) BCE term still has `secret_weight=20.0`, identical bit pressure to baseline. (3) Optimizer/cosine sched/grad-clip 5.0/AMP/resume logic untouched; AMP correctly disabled only around the loss to keep LPIPS-AlexNet in fp32. (4) `container.clamp(-1, 1)` retained before attack and loss — LPIPS/YUV/FFL inputs in expected range. (5) Test mini-train (200 steps) emitted `metrics.jsonl` with all new fields populated and `ckpt_latest.pt`; loss decreased monotonically (`l_quality` 1.83 -> 0.69), bit_acc rose to ~0.99 in clean+ramp phase. (6) `ImageSecretLoss` parameters/buffers correctly excluded from `model.state_dict()` (LPIPS frozen at construction).
**Strongest objection:** The fp32 wrapper around the loss call moves only the loss into fp32 but `container` and `cover` are detached-by-`.float()` from the autocast graph. Verified the gradient still flows back through the conversion: `container_fp32 = container.float()` is a differentiable cast of an autocast-produced tensor, so `loss.backward()` propagates through the cast into the fp16 INN graph correctly under `scaler.scale(loss).backward()`. No silent gradient break. Approved.
