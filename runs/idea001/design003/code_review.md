## design003
**Verdict:** APPROVED
**Automated gate:** PASS (review-check-implementation: structural PASS; check-scope PASS against parent baseline/; verify-claims PASS, 5 snippets)
**Algorithm fidelity:** Both prescribed edits present. (1) `losses.py:61-65` adds the LPIPS short-circuit exactly per design §4.1: `if self.lpips_w == 0.0: l_lpips = torch.zeros((), device=stego.device, dtype=stego.dtype) else: l_lpips = self.lpips(stego, cover).mean()`. The `self.lpips = lpips_lib.LPIPS(...)` construction at `losses.py:48` is preserved (idle but constructed) per §6, so other designs reusing the module get identical behavior when `lpips_w>0`. (2) `code/exp0_inn_train.py:258-262` instantiates `ImageSecretLoss(secret_weight=args.bit_weight, yuv_w=1.5, lpips_w=0.0, ffl_w=1.5)`. (3) Train-loop replacement at lines 390-402 is structurally identical to design001 (fp32 wrapper, `quality_alpha`, single call, no double-counted BCE). (4) Logging additions at lines 451-453.
**Training-signal sanity:** (1) Test mini-train confirms short-circuit fires: every step in `metrics.jsonl` shows `l_lpips: 0.0` while `l_yuv` and `l_ffl` are non-zero — AlexNet forward bypassed. (2) Wallclock per step ~0.18 s, slightly faster than design001's ~0.20 s, consistent with the LPIPS-skip claim. (3) `l_quality = 1.5*l_yuv + 0.0*l_lpips + 1.5*l_ffl` matches observed values (e.g. step 200: 1.5*0.069 + 1.5*0.036 = 0.158, observed `l_quality=0.158`). Math correct. (4) `secret_weight=20.0` preserved; bit_acc reaches 0.94 at step=200 in ramp phase. (5) Curriculum semantics, optimizer/scheduler/AMP/clip/resume all untouched. (6) `metrics.jsonl` and `ckpt_latest.pt` produced.
**Strongest objection:** The `torch.zeros((), device=stego.device, dtype=stego.dtype)` zero-scalar must be detachable in the `loss_logs["l_lpips"].detach()` log path and must not introduce dtype mismatches when added into `l_quality`. Verified: `losses.py:77` does `l_lpips=l_lpips.detach()` for logging, which works on any tensor including a zero scalar; and `l_quality = yuv_w*l_yuv + lpips_w*l_lpips + ffl_w*l_ffl` computes `0.0 * zero_scalar` which is a harmless 0 contribution in the same fp32 dtype as the other terms (loss is invoked under `autocast(enabled=False)` with fp32 inputs). No NaN/dtype risk. Approved.
